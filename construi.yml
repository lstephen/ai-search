image: maven:3-jdk-8

environment:
  - JENKINS_URL
  - BUILD_NUMBER
  - COVERALLS_REPO_TOKEN

targets:
  build: mvn -B package

  analytics:
    - mvn -B test
    - mvn -B jacoco:report coveralls:report

